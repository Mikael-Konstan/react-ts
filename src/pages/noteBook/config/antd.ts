import { Article, TagEnum } from './index.d';
import { tags } from './tags';

export const antdCompsArticles: Article[] = [
  {
    title: '树组件',
    path: '/antdComps/tree',
    tags: [tags[TagEnum.TREE]],
  },
];
